{% extends '@WebProfiler/Profiler/layout.html.twig' %}

{% block toolbar %}
    {% set icon %}
        <span class="sf-toolbar-status">{{ collector.calls|length }}</span>
    {% endset %}
    {% set text %}
        <div class="sf-toolbar-info-piece">
            <b>Calls</b>
            <span class="sf-toolbar-status">{{ collector.calls|length }}</span>
        </div>
    {% endset %}
    {{ include('WebProfilerBundle:Profiler:toolbar_item.html.twig', {
        link: profiler_url
    }) }}
{% endblock %}

{% block menu %}
{% endblock %}

{% block panel %}
{% endblock %}